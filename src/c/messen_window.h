#pragma once
#include <pebble.h>

// Das Menue "Messen": HRV (eine Minute, auf der Uhr gemessen) und SpO2 (der
// letzte Wert, den Kiesel-Helper aus Health Connect schickt).
void messen_window_zeige(void);
